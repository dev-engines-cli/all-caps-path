import {
  addToPATH,
  existsInPATH,
  removeFromPATH
} from '@/index.js';

describe('Index', () => {
  describe('addToPATH', () => {
    test('STUB: Does nothing', () => {
      expect(addToPATH())
        .toEqual(undefined);
    });
  });

  describe('existsInPATH', () => {
    test('STUB: Does nothing', () => {
      expect(existsInPATH())
        .toEqual(false);
    });
  });

  describe('removeFromPATH', () => {
    test('STUB: Does nothing', () => {
      expect(removeFromPATH())
        .toEqual(undefined);
    });
  });
});
